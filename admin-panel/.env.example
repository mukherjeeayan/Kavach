# Admin panel access key — MUST be set for the panel to load.
# Generate with: openssl rand -hex 32
VITE_ADMIN_ACCESS_KEY=CHANGE_ME_RANDOM_STRING

# Backend API URL
VITE_API_URL=http://localhost:3000
