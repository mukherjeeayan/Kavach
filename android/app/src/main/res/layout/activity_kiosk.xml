<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#1a1a2e">

    <!-- Lock Icon -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔒"
        android:textSize="72sp"
        android:layout_marginBottom="24dp" />

    <!-- Lock Type -->
    <TextView
        android:id="@+id/textLockType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#E31937"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <!-- Lock Reason -->
    <TextView
        android:id="@+id/textLockReason"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:paddingHorizontal="16dp" />

    <!-- Current Time -->
    <TextView
        android:id="@+id/textCurrentTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:textSize="48sp"
        android:textStyle="bold"
        android:layout_marginBottom="48dp" />

    <!-- Request More Time Button -->
    <Button
        android:id="@+id/btnRequestMoreTime"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Request More Time"
        android:textSize="16sp"
        android:textStyle="bold"
        android:backgroundTint="#2563eb" />

    <!-- Emergency SOS Button -->
    <Button
        android:id="@+id/btnEmergencySos"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="12dp"
        android:text="Emergency SOS"
        android:textSize="16sp"
        android:textStyle="bold"
        android:backgroundTint="#dc2626" />

    <!-- Help Text -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap 'Request More Time' to ask your parent for additional time"
        android:textColor="#888888"
        android:textSize="12sp"
        android:gravity="center"
        android:layout_marginTop="24dp" />

</LinearLayout>
