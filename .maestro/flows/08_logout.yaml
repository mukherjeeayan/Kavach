appId: com.safeguard.parentalcontrol
name: Logout
tags:
  - logout
  - auth
---
- launchApp
# NOTE: The Android app currently has no logout UI button.
# Logout is only available via ViewModel (no UI trigger).
# This flow verifies the app launches and the dashboard is visible.
# A logout button needs to be added to the dashboard for this flow to work.
- assertVisible: ".*Apps.*"
