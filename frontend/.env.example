# Backend API base URL (omit to use the Vite dev proxy /api/v1)
VITE_API_BASE_URL=

# Backend socket.io URL for realtime rule:changed pushes
# (e.g. http://localhost:3000 in dev)
VITE_SOCKET_URL=

# Mapbox public token (pk.…) — enables the embedded location map
VITE_MAPBOX_TOKEN=